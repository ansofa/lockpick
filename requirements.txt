# Core
flask>=3.0.0
flask-socketio>=5.3.6
eventlet>=0.35.0

# Raspberry Pi GPIO (proximity sensor)
# lgpio diinstall via: sudo apt install python3-lgpio
# (JANGAN install lgpio via pip — butuh swig untuk build)
gpiozero>=2.0

# Audio monitoring (USB microphone)
sounddevice>=0.4.6
numpy>=1.24.0
